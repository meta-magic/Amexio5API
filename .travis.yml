sudo: required
dist: trusty

#install google chrome, using addons
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
  sonarcloud:
    organization: "deepali-arvind-github" # the key of the org you chose at step #3
    token:
      secure: 081e2012239f75d61242a6d1d6605f0ee29dc019 # encrypted value of your token   

env:
  global:
language: node_js
node_js:
  - "8"
before_install:
  - composer config --global github-oauth.github.com "$GITHUB_TOKEN"
  
before_script:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - npm install --global gulp --save
  - gem install sass
script: 
  - npm install
  - npm install --save uglifyjs-webpack-plugin
  - npm install @angular-devkit/core --save
  - npm i node-sass --save-dev
  - npm i https://github.com/meta-magic/amexio-ios/blob/master/amexio-chart-d3.tgz?raw=true
  - npm i https://github.com/meta-magic/amexio-ios/blob/master/amexio-ng-extensions.tgz?raw=true
  - ng set defaults.styleExt scss
  - ng build --prod
  - git clone https://github.com/meta-magic/amexio-api-website-test.git
  - mkdir amexio-api-website-test/api/v5.3/*
  - cp -rf /home/travis/build/meta-magic/Amexio5API/dist/* amexio-api-website-test/api/v5.3/
  - cd amexio-api-website-test/
  - git add api
  - git commit -m "${COMMIT}"
  - git push --force --quiet "https://deepali-arvind:${GITHUB_TOKEN}@github.com/${GITHUB_REPO}.git" HEAD:master
  
after_success: 
  - codecov
cache:
  yarn: true
